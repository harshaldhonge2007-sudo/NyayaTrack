# NyayaTrack Environment Configuration
# Copy this file to .env for local overrides

# Optional: LLM Providers (Defaults to robust deterministic local fallback if omitted)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Optional: Frontend API URL (Defaults to same-origin /api for Vercel serverless)
NEXT_PUBLIC_API_URL=
